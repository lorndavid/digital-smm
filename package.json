{
  "name": "digitalsmm",
  "version": "1.0.0",
  "private": true,
  "description": "DigitalSMM - Social Media Marketing Platform",
  "license": "MIT",
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend,admin -c yellow,cyan,magenta \"npm run dev -w backend\" \"npm run dev -w frontend\" \"npm run dev -w admin\"",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:admin": "npm run dev -w admin",
    "build": "npm run build -w backend && npm run build -w frontend && npm run build -w admin",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w frontend && npm run typecheck -w admin",
  "start": "npm run start -w backend",
  "test": "npm run test -w backend",
  "loadtest": "npm run loadtest -w backend",
  "loadtest:multi": "npm run loadtest:multi -w backend",
  "test:e2e": "npm run test:e2e -w frontend"
},
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
